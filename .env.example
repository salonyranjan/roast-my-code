# Required — get one for free at https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Optional — raises GitHub's unauthenticated rate limit from 60/hr to 5000/hr.
# Create a fine-grained token with no permissions (public read is enough) at
# https://github.com/settings/tokens
GITHUB_TOKEN=
